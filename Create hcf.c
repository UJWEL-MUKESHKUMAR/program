#include <stdio.h>
 Void main()
   {
      Int i, num1, num2, min, hcf=1;
      printf("Enter two numbers \n");
      scanf("%d%d",&num1, &num2); 
      for(i=1;i<=min; i++)
       {
         /*if f is a factor of both */
         if(num1%i==0&& num2%i==0)
          {
            hcf=i;
           }
       }
   }
