#include <stdio.h>
 #include <math.h>
 void main ()
  {
    float sine, cosine, x; 
    printf("\n Enter the value :");
    scanf("%f",&x);
    sine=sin(x);
    cosine=cos(x);
    printf("\n sine value is %f",sine); 
    Printf("cos value is %f",cosine); 
 }
