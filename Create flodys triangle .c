#include <stdio. h>
 int main()
  {
    int row, I, j, num=1;
    printf("Enter number of rows\n");
    for(i=1;i<=rows; i++)
     { 
       for(j=1;j<=I;++j) 
        {
          printf("%d",num); 
          ++num; 
         }
     }
  } 
