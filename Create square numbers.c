#include <stdio. h>
 void main()
  {
    int i, n, sum=0;
    printf("input the number of terms\n");
    scanf("%d",&n); 
    printf("\n The square of natural number is:");
    for(i=1;i<=n; i++)
     {
       printf("%d",i*i);
     }
  }
